# =============================
# Database
# =============================
# Local example:
# MONGODB_URI=mongodb://localhost:27017/va
# Atlas example:
# MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/va?retryWrites=true&w=majority
MONGODB_URI=

# Server port (optional)
PORT=3000

# =============================
# AWS / S3 (for image uploads)
# =============================
# Required: the region where your S3 bucket lives
AWS_REGION=ap-southeast-2

# Required (for local dev): IAM access keys with permission to PutObject into your bucket.
# DO NOT commit real values. Put them in `.env` only.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Required: your S3 bucket name
AWS_S3_BUCKET=

# Optional: public base URL for serving images.
# If omitted, backend uses: https://{AWS_S3_BUCKET}.s3.{AWS_REGION}.amazonaws.com
# If you use CloudFront/custom domain, set it here, e.g.:
# AWS_S3_PUBLIC_BASE_URL=https://cdn.yourdomain.com
AWS_S3_PUBLIC_BASE_URL=

# =============================
# Auth (Customers)
# =============================
# Required: secret used to sign JWT tokens.
# Generate a long random string for production.
JWT_SECRET=

# Optional: token expiry (e.g. 1h, 7d)
JWT_EXPIRES_IN=7d

# =============================
# QStash (for scheduled emails)
# =============================
# Required for scheduled payment reminder emails in serverless environments.
# Get these from: https://console.upstash.com/qstash

# Your QStash token (from QStash dashboard)
QSTASH_TOKEN=

# Signing keys for webhook verification (from QStash dashboard -> Signing Keys)
QSTASH_CURRENT_SIGNING_KEY=
QSTASH_NEXT_SIGNING_KEY=

# =============================
# App Configuration
# =============================
# Required: The public URL of your backend API (used for QStash webhooks)
# e.g., https://your-backend.vercel.app or http://localhost:3000 for local dev
APP_BASE_URL=
